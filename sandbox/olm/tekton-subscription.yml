apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: tekton
  # !! the following namespace should be the same as where the catalogSource has been created
  namespace: marketplace
spec:
  channel: canary
  #startingCsv: openshift-pipelines-operator.v0.9.1
  installPlanApproval: Automatic
  # Name of the Package
  name: openshift-pipelines-operator
  source: community-operators
  # Namespace containing the sourcecatalog
  sourceNamespace: marketplace
